#exec command in Linux is used to execute a command from the bash itself. This command does not create a new process it just replaces the bash with the command
#to be executed. If the exec command is successful, it does not return to the calling process.
#Syntax:
#exec [-cl] [-a name] [command [arguments]] [redirectio